---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-main
  annotations:
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels: &labels
    environment: main
    owner: team-infra-sre
    project: "2063"
    organization: enterprise
    service: vault
spec:
  project: vault-argocd
  sources:
    - repoURL: https://github.com/LedgerHQ/sre-argocd.git
      targetRevision: main
      ref: values
    - repoURL: ghcr.io/ledgerhq/charts
      chart: argocd-appset-env
      targetRevision: "0.10.3"
      helm:
        passCredentials: true
        valueFiles:
          - "$values/deploy/platform-2220-cluster/applications/vault/releases/release-main.yaml"
          - "$values/deploy/platform-2220-cluster/applications/vault/main/values.yaml"
        valuesObject:
          appproject:
            roles:
              - name: admin-vault-stg
                description: Add admin permission to stg envs
                policies:
                  - p, proj:vault-main:admin-vault-stg, exec, create, vault-main/*, allow
                  - p, proj:vault-main:admin-vault-stg, applications, update, vault-main/*, allow
                  - p, proj:vault-main:admin-vault-stg, applications, delete, vault-main/*, allow
                  - p, proj:vault-main:admin-vault-stg, applications, override, vault-main/*, allow
                  - p, proj:vault-main:admin-vault-stg, applications, action/*, vault-main/*, allow
                  - p, proj:vault-main:admin-vault-stg, exec, create, vault-main/*, allow
                groups:
                  - infra-app-green-rundeck-vault-user-stg
                  - infra-group-vault-tech-leads
          labels: *labels
          destination:
            name: shared-application-2021-cluster
            namespace: 2063-vault-main
  destination:
    name: in-cluster
    namespace: argocd-central
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - Validate=true
      - ServerSideApply=true
